name: 'tableau'
version: '1.0.0'
profile: 'tableau'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  tableau:

    # Default materialization
    +materialized: view

    staging:
      +materialized: view

    intermediate:
      +materialized: view

    facts:
      +materialized: view

    marts:
      +materialized: table

seeds:
  tableau:
    +quote_columns: false

    tableau_site_agency:
      +column_types:
        site_id: integer
        site_agency_acronym: varchar(10)